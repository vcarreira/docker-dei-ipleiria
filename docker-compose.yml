version: '2'

services:
  cdev:
    container_name: cdev
    image: pa
    build:
      context: ./docker
      dockerfile: pa.dockerfile
    expose:
      - 1234
    volumes:
      - ./:/home/pa
    networks:
      - pa-network

networks:
  pa-network:
    driver: bridge
